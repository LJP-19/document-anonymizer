# Optional: en_core_web_trf as an alternative spaCy NER backend.
#
# NEVER bundled by default and NEVER the production model without
# DOCANON_SPACY_MODEL=en_core_web_trf set explicitly. See the comment on
# MODEL_NAME in app/detection/ner.py for the full reasoning: this costs
# roughly 1-1.5 GB installed (model + spacy-transformers + a PyTorch CPU
# runtime) versus en_core_web_sm's ~15 MB, and is an order of magnitude
# slower per page on CPU-only hardware, which this desktop app has to
# assume. Install only to measure it yourself against real documents:
#
#   pip install -r requirements-trf.txt
#   python -m spacy download en_core_web_trf
#   DOCANON_SPACY_MODEL=en_core_web_trf python -m app.cli process ...
spacy-transformers>=1.3
torch>=2.0
